# Frontend / Otaku-style scripts

Scripts copied from [elizaOS/otaku](https://github.com/elizaOS/otaku) for reference. Run from repo root or adjust paths as needed.

| Script                | Purpose               |
| --------------------- | --------------------- |
| `patch-plugin-sql.ts` | SQL plugin patching   |
| `setup-pg-cron.ts`    | PostgreSQL cron setup |
| `test-x402-jobs.ts`   | x402 jobs testing     |

Project-level scripts (db, deploy, test) live in the repo root `scripts/` folder.
