# Dependencies
node_modules/

# Python virtual environment (do not commit)
.venv/
venv/

# Build output
dist/
*.tsbuildinfo

# Environment (secrets)
.env
.env.local
.env.*.local

# Eliza / DB
.eliza/
.elizadb/
.elizadb-test/
# Local PGLite/Postgres backup artifacts (rotating snapshots, raw clusters)
.eliza-backups/
# Some legacy backup naming patterns (kept for backward compatibility)
.elizadb.bak*/
.eliza/.elizadb.bak*/

# OpenClaw research cache (ephemeral, runtime-generated)
.openclaw-cache/

# Plugin-vince scripts: generated openclaw-queue / standup output (train_models from scripts dir)
src/plugins/plugin-vince/scripts/docs/



# Standup plugin test artifacts (temporary dirs from test runs)
standup-deliverables-test-*/
standup-deliverables-build-test-*/

# IDE / OS
.DS_Store
*.log
.idea/
.vscode/

# Otaku reference clone (mirror patterns only; do not commit)
otaku-ref/

# Local-only data (e.g. Solus current positions); never commit
local/

# Knowledge base — personal RAG corpus; not committed to public repo.
# Agents load this at runtime from the local filesystem.
# To share knowledge publicly, maintain a separate knowledge-public/ directory.
knowledge/

# Cursor plan files — internal task planning; not public
.cursor/plans/

# tasks/todo.md — operational notes; not public
tasks/todo.md

cache_dexter/
cache_aifh/
cache_aihf/

# X bookmarks → Pine pipeline (Rust) — local output + SQLite cache
data/x-bookmarks-pipeline/
