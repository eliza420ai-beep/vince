# Dependencies
node_modules/

# Python virtual environment (do not commit)
.venv/
venv/

# Build output
dist/
*.tsbuildinfo

# Environment (secrets)
.env
.env.local
.env.*.local

# Eliza / DB
.eliza/
.elizadb/

# OpenClaw research cache (ephemeral, runtime-generated)
.openclaw-cache/

# Plugin-vince scripts: generated openclaw-queue / standup output (train_models from scripts dir)
src/plugins/plugin-vince/scripts/docs/



# Standup plugin test artifacts (temporary dirs from test runs)
standup-deliverables-test-*/
standup-deliverables-build-test-*/

# IDE / OS
.DS_Store
*.log
.idea/
.vscode/

# Otaku reference clone (mirror patterns only; do not commit)
otaku-ref/

# Local-only data (e.g. Solus current positions); never commit
local/

# Private Hypersurface sizing file for Solus (gitignored but loaded at runtime)
knowledge/private/solus-options-sizing.md

# Optional: uncomment if you don't want to track knowledge build artifacts
# knowledge/**/*.pdf

# Summarize slide extraction output (images); remove to commit slides
knowledge/.slides/

.openclaw-cache/
